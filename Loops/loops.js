// 1. Print every number in an array.

const numbers = [5, 10, 15, 20, 25];

numbers.forEach(function(number){
    console.log(number)
})

// 2. Print every name in an array.

const names = ["Alice", "Bob", "Charlie", "David"];

names.forEach(function(name){
    console.log(name)
})

// 3. Print each fruit in uppercase.

const fruits = ["apple", "banana", "orange"];

fruits.forEach(function(fruit){
    console.log(fruit.toUpperCase())
})

// 4. Find the sum of all numbers.

const numbers1 = [2, 4, 6, 8, 10];

numbers.forEach(function(number){
    console.log(numbers1[0] + numbers1[1] + numbers1[2] + numbers1[3] + numbers1[4]);
})

// 5. Count how many items are in the array using a foreach loop.

const sums = [2, 4, 6, 8, 10];

sums.forEach(function(sum){
    console.log(sums.length)
})

// 6. Find the largest number in an array.

const maximum = [12, 45, 7, 89, 23];

maximum.forEach(function(max){
    console.log(Number(max[maximum])
})

// 7. Count how many numbers are even.

const evens = [1, 2, 3, 4, 5, 6, 7, 8];

evens.forEach(function(even){
    if(evens % 2 = 0){
        console.log(evens)
    }
})

